<!--pages/home/home.wxml-->
  <s-search />
  <s-swiper imgs="{{swiperList}}" />
  <s-storeInfo />
  <view class="hotGoods" wx:for="{{hotGoods}}" wx:key="key" >
    <s-hot goods="{{item}}" bindgoDetail= "goDetail" />
    <view class='flag'>hot</view>
  </view>